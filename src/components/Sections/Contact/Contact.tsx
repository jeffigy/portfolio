import React from "react";
import CustomSection from "../CustomSection";

type ContactProps = {};

const Contact: React.FC<ContactProps> = () => {
  return (
    <CustomSection sectionTitle="Contact" sectionName="contact">
      Have a good coding
    </CustomSection>
  );
};
export default Contact;
